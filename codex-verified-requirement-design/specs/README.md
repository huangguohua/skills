# specs/

本目录为占位。实际需求文档与循环日志按 `config.json` 的 `output.output_dir`（默认 `specs-mcp/<feature>/`）落盘：

- `specs-mcp/<feature>/requirements.md` — 结构化需求规格（经 codex 复审 CLEAR）
- `specs-mcp/<feature>/requirement-review-loop.md` — 每轮复审日志
